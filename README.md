# PodChat

PodChat is a Next.js app for listening to podcasts, browsing transcripts, and chatting with an AI host built from podcast context.

## Local Development

```bash
bun install
bun dev
```

The app runs at `http://localhost:3000`.
